interface:
  display_name: "using-solo"
  short_description: "Route Solo-managed work through agent orchestration"
  default_prompt: "Use $using-solo to detect whether this is a Solo-managed lead session and apply its orchestration workflow."

dependencies:
  tools:
    - type: "mcp"
      value: "solo"
      description: "Solo MCP server for session detection and agent coordination"

policy:
  allow_implicit_invocation: true
